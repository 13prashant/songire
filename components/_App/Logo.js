const Logo = () => {
  return (
    <h3
      style={{
        color: 'var(--whiteColor)',
        textShadow: '2px 2px var(--mainColor)',
        margin: '0',
      }}
    >
      SONGIRE
    </h3>
  );
};

export default Logo;
